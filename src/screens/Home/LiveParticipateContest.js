import React from 'react';
import { Text, View, ScrollView, SafeAreaView, TouchableOpacity, Image, Dimensions, Platform, } from 'react-native';

import { useSelector } from 'react-redux'

const LiveParticipateContest = () => {

    return (
        <View>
            <Text>LiveParticipateContest</Text>
        </View>
    )
}

export default LiveParticipateContest
