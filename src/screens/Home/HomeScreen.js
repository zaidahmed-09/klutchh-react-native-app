import React from 'react';
import { Text, View, ScrollView, SafeAreaView, TouchableOpacity, Image, Dimensions, Platform, } from 'react-native';

import { useSelector } from 'react-redux'

const HomeScreen = () => {

    return (
        <View>
            <Text>HomeScreen</Text>
        </View>
    )
}

export default HomeScreen